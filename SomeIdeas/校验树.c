

RES CheckTreeCheck(*NetworkBuff)
{
    if(Prptocol == IP)
    {
        CheckIPChecksum()
    }
}

def CheckTreeCheck(NetworkBuff):
    if == IP:
        def CheckIPChecksum():
            if == UDP:
                def CheckUDP():
                    ***
            if == TCP:
                def CheckTCP():
                    ***
            CheckIP***

def FillCheckSum(NetworkBuff):
    if == IP:
        if == UDP:FillUDPChecksum()
        if == TCP:FillTCPChecksum()
        fillIPChecksum()





